<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
    <div> List Commodity di Pasar Bandung Th 2017 </div>
    {{ $group := "" }}
    {{ range $item := . }}
        {{if noteq .Location $group }}
            Lokasi: {{ .Location }}
            {{ $group = .Location }}
        {{end}}
        <div>{{ .Name }}</div>
        <div>{{ $item.marketLocation }}</div>
    {{ end }}
    <p> {{template "commodity2"}} </p>
</body>
</html>